# Base URL of the WordPress backend (no trailing slash).
# The frontend calls: $NEXT_PUBLIC_WP_URL/wp-json/powpills/v1/homepage
NEXT_PUBLIC_WP_URL=http://localhost:8080

# Seconds to cache the WordPress response. 0 disables caching.
WP_REVALIDATE_SECONDS=60
